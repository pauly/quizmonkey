module.exports = [
  require('./oscars-best-picture'), // include the full data
  // // require('./oscars-best-actress')[0]], // include only the title
  [require('./oscars-best-actor')[0]],
  [require('./italian')[0]],
  // [require('./german')[0]],
  // // require('./more-german')[0]],
  [require('./periodic-table')[0]]
]
